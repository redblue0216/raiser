from setuptools import setup,find_packages

setup(
        name = 'Raiser',
        version = '0.1',
        packages = find_packages(),
        author = 'shihua',
        description = 'Raiser Module'
)